import { LocationsPipePipe } from './locations-pipe.pipe';

describe('LocationsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LocationsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
